Role: Senior Full-Stack MERN DeveloperProject: SehatSaathi AI (Mobile-First Web App)Layout: 390px centered mobile-responsive web view.🏗️ Tech StackFrontend: React.js (Vite), Tailwind CSS.Backend: Node.js, Express.js.Database: MongoDB Atlas (Mongoose ODM).Auth: Custom JWT-based Phone Auth via Twilio OTP.Hardware APIs: Web Camera API, Geolocation API, Web Speech API.📦 Phase 1: MERN Foundation & AuthDatabase Schema (Mongoose):User: name, phone (unique), age, language, emergency_contacts (array of objects), profileCompleted.Reminder: userId, medName, time, frequency, stockCount, status (taken/pending).HealthData: userId, type (prescription/report), imageUrl, extractedText.Custom Phone Login:Route POST /api/auth/send-otp: Use Twilio Verify to send a code to the user's phone.Route POST /api/auth/verify-otp: Verify the code, create/find user, and return a JWT Token.Hardcoded Dev OTP: Allow Phone 8446530525 and OTP 12345 to bypass Twilio during UI building.🤖 Phase 2: AI & Web Hardware (The "Real" Features)Webcam Pill Scan: Use navigator.mediaDevices.getUserMedia to stream to a <video> tag. Capture frame $\rightarrow$ send to Backend $\rightarrow$ Analysis API.Web Speech Assistant: Implement a global "Listen" button. Use window.speechSynthesis to read the specific aria-labels or text content of the current view in the user's chosen language (Hindi, Marathi, etc.).Real-Time Reminders: Create a frontend useEffect hook that checks the reminders state every minute. If currentTime == reminderTime:Trigger window.alert.Play a local .mp3 alarm sound.Use navigator.vibrate([500, 110, 500]).🚨 Phase 3: Twilio SMS & SOS SystemSOS Trigger: On click, fetch navigator.geolocation.getCurrentPosition. POST the coordinates to /api/sos.Twilio Backend: The server must loop through the user's emergency_contacts and send a real SMS: "EMERGENCY! [Name] needs help. Location: https://maps.google.com/?q=lat,lng".Low Stock Alert: When a user marks a pill as "Taken" and stockCount reaches 1, the backend automatically triggers a Twilio SMS to the primary contact.🎨 Phase 4: Elder-First UI RulesContainer: Everything inside a <div class="max-w-[390px] mx-auto min-h-screen bg-white shadow-xl">.Visuals: Background #F5F7FA, Primary Buttons #1565C0 (Blue), SOS Button #D32F2F (Red).Font: Base size 20px, all headers bold.Navigation: Fixed bottom navbar: Home | Scan | SOS. Each page must have a Large Back Button.🛠️ Replit Implementation Steps:Secrets: Store MONGODB_URI, JWT_SECRET, TWILIO_SID, TWILIO_AUTH_TOKEN, and TWILIO_NUMBER in Replit Secrets.API Folder: Create a server/ folder for Express and a src/ folder for React.Reminders: Since browsers throttle background tabs, advise the user to keep the tab open or use a Service Worker with Web Push Notifications for 100% reliability.